function multiply(x, y) {
    const result = x * y;
    return result;
}

function main() {
    const num1 = 5;
    const num2 = 10;
    const product = multiply(num1, num2);
    console.log(`The product is: ${product}`);
}

main();
